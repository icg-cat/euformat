% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{eu_gg_labels}
\alias{eu_gg_labels}
\alias{eu_scale_y}
\alias{eu_scale_x}
\title{Axis labelling helpers for ggplot2 using EU-style formatting}
\usage{
eu_gg_labels(digits = 2, currency = FALSE)

eu_scale_y(digits = 2, currency = FALSE)

eu_scale_x(digits = 2, currency = FALSE)
}
\arguments{
\item{digits}{Number of decimal digits to show.}

\item{currency}{Logical; if TRUE, append `" €"` after formatting.}
}
\value{
A ggplot2 scale object.
}
\description{
These functions provide convenient ggplot2 scales that format axis
labels using European number conventions (comma decimals, dot thousands),
and optional currency symbols. Formatting respects the global toggle
(`eu_format_on()` / `eu_format_off()`).
}
\examples{
library(ggplot2)

# Turn on EU-style formatting
eu_format_on()

ggplot(mtcars, aes(x = hp, y = mpg)) +
  geom_point() +
  eu_scale_y() +
  eu_scale_x()

# Turn formatting off again (optional cleanup for session)
eu_format_off()
}
